# ln ToolKit.semple.service /etc/systemd/system/ToolKit.service -s

[Unit]
Description=Telegram bot ToolKit
After=syslog.target
After=network.target

[Service]
Type=simple
User= # Your user name
WorkingDirectory= # Path to git repo
ExecStart=python3.9 main.py -m
RestartSec=5
Restart=always
 
[Install]
WantedBy=multi-user.target